[build]
  command = "bundle install && bundle exec jekyll build --trace"
  publish = "_site"

[build.environment]
  RUBY_VERSION = "3.1"
  JEKYLL_ENV = "production"
  # Disable mise auto-installation
  MISE_DISABLED = "1"